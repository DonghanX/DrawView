package com.redhoodhan.draw.draw_option.data

import android.graphics.Color

class DrawConst {
    companion object {
        const val DEFAULT_MIN_STROKE_WIDTH = 3F
        const val DEFAULT_STROKE_WIDTH = 15F
        const val DEFAULT_PAINT_COLOR = Color.BLACK
        const val DEFAULT_CANVAS_COLOR = Color.WHITE
        const val MAX_STROKE_WIDTH_BIAS = 5F
        const val CHISEL_ALPHA = 80
        const val MAX_ALPHA = 255
    }
}
